!!!
%html{:lang => "ja"}
  %head
    %meta{:charset => "utf-8"}
    %title entakun
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}
    %meta{:content => "", :name => "description"}
    %meta{:content => "", :name => "author"}
    / Le styles
    %link{:href => url_for('/assets/bootstrap.css'), :rel => "stylesheet"}
      :css
        body {
          padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
        }

    %link{:href => url_for('/assets/application.css'), :rel => "stylesheet"}
    = yield_content :css

    %link{ rel: "shortcut icon", href: url_for('/favicon.png'), type: "image/x-icon" }
    %link{ rel: "icon", href: url_for('/favicon.png'), type: "image/x-icon" }

    = yield_content :head

    / Le HTML5 shim, for IE6-8 support of HTML5 elements
    /[if lt IE 9]
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>

  %body
    = yield
