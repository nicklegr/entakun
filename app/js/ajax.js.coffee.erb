setup_ajax_error_handler = () ->
  $('.alert').ajaxError((event, request, settings) ->
    url_body = settings.url.replace(/\?.+/, '')

    $(this).text('通信エラーが起きました。ページを再読み込みしてやり直してください (' + url_body + ')')
    $(this).show()
  )
